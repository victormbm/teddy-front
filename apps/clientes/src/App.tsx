import { Button } from "design-system";

function App() {
  return (
    <div>
      <h1>Clientes</h1>
      <Button onClick={() => alert("Clicou!")}>Clique Aqui</Button>
    </div>
  );
}

export default App;
